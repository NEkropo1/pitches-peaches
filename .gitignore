.venv/
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/
.env
runs/
.DS_Store
.coverage
htmlcov/

# Personal CVs. Never in the repository, even by accident — `git add -A`
# is one keystroke and this directory is where people drop their own.
*.pdf
!examples/**/*.pdf

# A workspace created by running `peaches` inside the checkout. Personal:
# your CV, and dossiers about jobs you are applying to.
cvs/
applications/
