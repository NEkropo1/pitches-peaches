This is an enterprise or scale-up process, so it is legible and repeatable.
Predict it and say what each round is really for.

Fill `rounds` with the sequence you expect — typically some subset of: recruiter
screen, hiring manager call, technical screen, system design, coding exercise
or take-home, behavioural, and a final loop or panel. Use what the recon found
about their actual process where it is known; predict the rest from the
company's size, stage, and sector, and say which parts are predicted.

For each round:

- `what_is_assessed` — the thing behind the thing. A system design round at a
  company with a reliability problem is assessing whether you have carried a
  pager, not whether you can draw boxes.
- `how_to_run_it` — how large organisations actually conduct this round: who is
  in the room, what the rubric they are scoring against tends to contain, how
  long they will let you talk before interrupting, what a "no hire" note in
  this round usually says. Then what to do with that.

Set `branch` to `structured`. Leave `interviewer_profiles` and
`pitch_adaptation` empty unless the recon named specific people — at this size,
the process matters more than the person, and guessing at individuals is noise.
